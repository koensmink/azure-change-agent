services:
  collector:
    build: ./collector
    container_name: ms-changes-collector
    env_file:
      - ./.env
    ports:
      - "8088:8088"
    volumes:
      - ./collector/data:/app/data
    restart: unless-stopped
